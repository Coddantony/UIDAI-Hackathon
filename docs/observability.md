# Observability

The service should expose health state separately from readiness state and propagate a request correlation ID through API responses. Authentication, verification, and audit events should be measurable without logging secrets or identity payloads.
