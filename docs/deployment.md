# Deployment Notes

Run the API behind a TLS-terminating reverse proxy, provide MongoDB and JWT settings through the environment, restrict CORS to trusted clients, and expose the health endpoints to the platform health checker.
