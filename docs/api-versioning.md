# API Versioning

The preferred API surface is under `/api/v1`. Existing unversioned routes are retained only for backward compatibility. New clients should use the versioned routes so future breaking changes can be introduced without disrupting existing integrations.
